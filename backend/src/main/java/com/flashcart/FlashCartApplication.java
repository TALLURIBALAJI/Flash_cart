package com.flashcart;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Flash-Cart Backend Application Entry Point
 * 
 * Spring Boot application for managing collaborative expense splitting.
 */
@SpringBootApplication
public class FlashCartApplication {

    public static void main(String[] args) {
        SpringApplication.run(FlashCartApplication.class, args);
    }
}
